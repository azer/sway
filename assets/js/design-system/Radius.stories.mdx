import { theme } from '../themes'
import { Section, RadiusToken } from './elements'
import { Meta } from '@storybook/addon-docs';

<Meta title="Design Tokens/Radius" />

# Radius

<Section title="Radius Tokens" desc="Tokens for controlling roundness of an element.">
	 {Object.keys(theme.radii).map((key) => <RadiusToken token={key} value={theme.radii[key].value} />)}
</Section>
