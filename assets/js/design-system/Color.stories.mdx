import { Meta } from '@storybook/addon-docs';
import { theme } from '../themes';
import { colors, ColorToken, Section } from './elements';

<Meta title="Design Tokens/Colors" />

# Colors

<Section title="Primary Color">
	 {(colors().primary || []).map((c) => <ColorToken token={c.token} value={c.value} />)}
</Section>


<Section title="Base Colors" >
	 {(colors().base || []).map((c) => <ColorToken token={c.token} value={c.value} />)}
</Section>
