{
  "name": "Bafa",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "devDependencies": {
    "@types/electron-devtools-installer": "^2.2.1",
    "concurrently": "^7.0.0",
    "cross-env": "^7.0.3",
    "electron": "^17.0.0",
    "electron-builder": "^22.14.13",
    "electron-devtools-installer": "^3.2.0",
    "electron-is-dev": "^2.0.0",
    "electron-reload": "^2.0.0-alpha.1",
    "wait-on": "^6.0.0"
  },
  "scripts": {
    "dev": "concurrently \"cross-env BROWSER=none  tsc -p electron -w\" \"wait-on http://localhost:4000 && tsc -p electron && electron .\"",
    "build": "npm run build && tsc -p electron && electron-builder"
  },
  "author": "",
  "license": "ISC"
}
